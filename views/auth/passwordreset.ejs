<!DOCTYPE html>
<html lang="en">
  <%- include('../common/head.ejs') %>
  <body style="background: linear-gradient(135deg, #667eea, #764ba2); color: white; font-family: Arial, sans-serif;">
    <!-- Flash Messages -->
    <% if (sucess && sucess != '') { %>
    <div
      class="alert alert-success text-center"
      style="
        position: absolute;
        top: 50px;
        left: 50%;
        transform: translateX(-50%);
        width: 50%;
        background-color: #d4edda;
        color: #155724;
        border-radius: 5px;
        padding: 10px;
        z-index: 1000;
      "
    >
      <%= sucess %>
    </div>
    <% } else if (err && err != '') { %>
    <div
      class="alert alert-danger text-center"
      style="
        position: absolute;
        top: 50px;
        left: 50%;
        transform: translateX(-50%);
        width: 50%;
        background-color: #f8d7da;
        color: #721c24;
        border-radius: 5px;
        padding: 10px;
        z-index: 1000;
      "
    >
      <%= err %>
    </div>
    <% } %>

    <!-- Login Form Section -->
    <div class="d-flex justify-content-center align-items-center" style="min-height: 100vh; padding: 20px;">
      <div
        style="
          background: white;
          padding: 30px;
          border-radius: 10px;
          box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
          max-width: 400px;
          width: 100%;
          text-align: center;
        "
      >
        <h2 style="color: #4a4a8c; font-size: 26px; font-weight: bold;">🔐 Verify Your Email</h2>
        <p style="color: #666; font-size: 14px; margin-bottom: 20px;">Enter your email address to receive the verification link.</p>
        <form action="<%= generateUrl('resetpasswordlinkcreate')%>" method="POST">
          <!-- Email Field -->
          <div style="margin-bottom: 20px">
            <label for="email" style="display: block; font-weight: bold; color: #555;">Email Address</label>
            <input
              type="email"
              id="email"
              name="email"
              placeholder="Enter your email"
              required
              style="
                width: 100%;
                padding: 12px;
                border: 1px solid #ccc;
                border-radius: 5px;
                font-size: 16px;
                text-align: center;
              "
            />
          </div>
          <!-- Submit Button -->
          <button
            type="submit"
            style="
              width: 100%;
              padding: 12px;
              background: linear-gradient(135deg, #ff758c, #ff7eb3);
              color: white;
              border: none;
              border-radius: 5px;
              font-size: 18px;
              font-weight: bold;
              cursor: pointer;
              transition: 0.3s;
            "
          >
            📩 Send Verification Link
          </button>
        </form>
      </div>
    </div>
  </body>
</html>
