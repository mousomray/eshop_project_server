<!DOCTYPE html>
<html lang="en">
  <%- include('../common/head.ejs')%>
  <body id="page-top" style="background: linear-gradient(135deg, #667eea, #764ba2); color: white; font-family: Arial, sans-serif;">
    <!-- Page Wrapper -->
    <div id="wrapper">
      <!-- Sidebar -->
      <%- include('../common/sidebar.ejs')%>
      <!-- End of Sidebar -->

      <!-- Content Wrapper -->
      <div id="content-wrapper" class="d-flex flex-column">
        <!-- Main Content -->
        <div id="content">
          <!-- Topbar -->
          <%- include('../common/navbar.ejs')%>
          <!-- End of Topbar -->

          <!-- Begin Page Content -->
          <div class="container-fluid">
            <div class="text-center mb-4">
              <a href="<%= generateUrl('addbrand')%>" class="btn btn-primary btn-lg shadow">➕ Add Brand</a>
            </div>

            <% if (sucess && sucess != '') { %>
            <div class="alert alert-success text-center"> <%= sucess %> </div>
            <% } else if (err && err != '') { %>
            <div class="alert alert-danger text-center"> <%= err %> </div>
            <% } %>

            <!-- DataTales Example -->
            <div class="card shadow-lg border-0">
              <div class="card-header bg-primary text-white text-center">
                <h5 class="m-0 font-weight-bold">🏷️ Total Brands: <%=brands.length%></h5>
              </div>
              <div class="card-body">
                <div class="table-responsive">
                  <table class="table table-hover text-center" id="dataTable" width="100%" cellspacing="0">
                    <thead class="bg-dark text-white">
                      <tr>
                        <th>Brand Name</th>
                        <th>Edit</th>
                        <th>Delete</th>
                      </tr>
                    </thead>
                    <tbody class="bg-light">
                      <% brands.forEach((value) =>{ %>
                      <tr>
                        <td><b class="text-primary"><%=value.name%></b></td>
                        <td>
                          <a href="<%= generateUrl('editbrand', { id: value._id }) %>" class="btn btn-success btn-sm shadow">✏️ Edit</a>
                        </td>
                        <td>
                          <a href="<%= generateUrl('deletebrand', { id: value._id }) %>" class="btn btn-danger btn-sm shadow">🗑️ Delete</a>
                        </td>
                      </tr>
                      <% }) %>
                    </tbody>
                  </table>
                </div>
              </div>
            </div>
          </div>
          <!-- /.container-fluid -->
        </div>
        <!-- End of Main Content -->

        <!-- Footer -->
        <%- include('../common/footer.ejs')%>
        <!-- End of Footer -->
      </div>
      <!-- End of Content Wrapper -->
    </div>
    <!-- End of Page Wrapper -->

    <!-- Scroll to Top Button-->
    <a class="scroll-to-top rounded" href="#page-top">
      <i class="fas fa-angle-up"></i>
    </a>

    <!-- Logout Modal-->
    

    <!-- JS file area-->
    <%- include('../common/vanilajs.ejs')%>
  </body>
</html>
